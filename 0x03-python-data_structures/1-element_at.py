#!/usr/bin/python3
def element_at(my_list=[]):
    for x in my_list:
        if x < 0:
            return None
        elif x > idx:
            return None
        else:
            return(my_list[idx])
